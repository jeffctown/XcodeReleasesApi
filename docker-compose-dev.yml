version: "3.3"
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: xcodereleases:"1.0.0"
    environment:
        env: "dev"
    ports:
      - 8080:8080
    volumes:
      - .:/app
    working_dir: /app
    stdin_open: true
    tty: true
    entrypoint: bash
